#pragma once

// This is a pure C++ module. The header is intentionally minimal
// and only serves as an entry point for prefab publishing.
// Actual implementation is done through the TurboModule system.

#include "LlamaCppRnModule.h" 